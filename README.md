# RemotePlay

## Starting

- Prepare virtualenv
- Restore packages via `pip`
- Open two bash's
- `source env/bin/activate` in each
- 1 - python game.py
- 2 - python control.py
- Access controls at `http://localhost:5000`
